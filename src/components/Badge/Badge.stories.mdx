import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Badge } from './index';

<Meta title="Badge" component={Badge} />

export const Template = args => <Badge {...args} />;

# Badge

Badge shows the text inside rounded element with colored background

## Badge with content isnide

<Canvas>
  <Story name="primary" args={{ color: 'primary', children: 'primary' }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary" args={{ color: 'secondary', children: 'secondary' }}>
    {Template.bind({})}
  </Story>
  <Story name="Warning" args={{ color: 'warning', children: 'warning' }}>
    {Template.bind({})}
  </Story>
  <Story name="Info" args={{ color: 'info', children: 'Info' }}>
    {Template.bind({})}
  </Story>
  <Story name="Error" args={{ color: 'error', children: 'Error' }}>
    {Template.bind({})}
  </Story>
  <Story name="Success" args={{ color: 'success', children: 'Success' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Badge with empty content

<Canvas>
  <Story name="emptyPrimary" args={{ color: 'primary' }}>
    {Template.bind({})}
  </Story>
  <Story name="emptySecondary" args={{ color: 'secondary' }}>
    {Template.bind({})}
  </Story>
  <Story name="emptyWarning" args={{ color: 'warning' }}>
    {Template.bind({})}
  </Story>
  <Story name="emptyInfo" args={{ color: 'info' }}>
    {Template.bind({})}
  </Story>
  <Story name="emptyError" args={{ color: 'error' }}>
    {Template.bind({})}
  </Story>
  <Story name="emptySuccess" args={{ color: 'success' }}>
    {Template.bind({})}
  </Story>
</Canvas>